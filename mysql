Be&y59ljiwY>
6
mysql -h ip -P port -u user -p 
输入密码

7
1.数据库基础
mysql实际上是一种网络服务 
mysqld:服务端 d表示守护进程 daemon守护进程。
mysql: 客户端
mysql本质上基于C(mysql)S(mysqld)模式的一种网络服务

查看mysql的端口号
netstat -nltp
Proto Recv-Q Send-Q Local Address           Foreign Address         State       PID/Program name    
tcp6       0      0 :::3306                 :::*                    LISTEN      5673/mysqld         

2数据库服务
mysql是一套给我们提供数据存取服务的网络程序。

数据库一般指的是，在磁盘或者内存中存储的特定结构组织的数据。
将来磁盘上存储的一套数据库方案。
数据库--磁盘
数据库服务--mysqld

3为什么使用mysql存储
存储数据的时候，文件就可以了，为什么还需要数据呢？
  1.一般的文件确实给我们提供了数据的存储功能，但是文件并没有提非常好的数据管理能力(用户角度)。数据管理，数据内容。 文件只是rwx,不是数据的内容的权限管理。这样的代码太难得写了。
  2.数据库本质：对数据内容存储的一套解决方案。 你给我字段或者要求，我直接给你结果就行。我就是mysqld.
对数据内容进行更加细腻度的操作。

文件缺点
  安全性问题
  查询和管理
  存储不利
  控制不方便

存储解决方案。
  客户端     服务器         磁盘
  mysql      mysqld         数据库文件

8
见见数据库
1.建立一个数据库
2.建立一张表结构
3.插入数据
看看在Linux是如何工作的

create database helloworld; 下面多了一个文件目录
/var/lib/mysql/helloworld

1.建立数据库，本质就是linux下的一个目录 /var/lib/mysql/
  create database helloworld;
2.使用表
  use helloworld; 类似cd指令
3.创建表，linux下创建一个文件即可
  mysql> create table student
    ->(
    -> name varchar(32),
    -> age int,
    -> gender varchar(2)
    -> );

4.数据插入
insert into student (name,age,gender) values('zhangs',11,'m');
5.查看数据
	select * from student;
  
现在只需要sql指令，操作数据，具体操作mysqld帮你维护。
数据库本质也是文件，只不过这些文件并不有程序员直接操作，mysqld帮你操作。

9
常见数据库类型
Michael Widenius
  My Structured Query Language
redis

10
mysql的工作关系原理
  mysql   mysqld         数据库
                         DB    表(二进制文件)
  Client  MySqld服务端   DB    表
                         DB    表
mysqld:服务端的守护进程。


11
  MySql数据逻辑存储:excel。逻辑存储和物理存储不一样的。
+--------+------+--------+
| name   | age  | gender |
+--------+------+--------+
| zhangs |   11 | m      |
| zhangs |   11 | m      |
| zhangs |   11 | m      |
+--------+------+--------+
3 rows in set (0.00 sec)


MySql架构(mysqld的架构)
   client
  Connect Pool  安全和链接管理
  翻译sql语句   词法分析和语法分析
  存储引擎(类似计算机体结构的驱动) 应用层。对应不同的种类数据类型。数据存储方案
  文件系统。内核层。 对应的文件系统。
MySql其实是一个文件系统
在OS之上的文件系统。用户--->mysql--->实际的操作系统的文件系统。
OS上面的三层就是用户层 下面OS
应用层，网络层。


12
SQL语句分类
DDL data definition language 数据定义语言
  create drop alter

DML data manipulation language 数据操作语言，操作数据
  insert delete update
  DQL:数据查询语言

DCL data control language  数据控制语言,数据权限和事务
  grant revoke commit


13
存储引擎是：数据库管理系统如何存储数据、如何为存储的数据建立索引和如何更新、查询数据等技术
的实现方法。

show engines;
innodb存储引擎。

总结：
  1.数据库是什么
  2.mysql
  3.mysqld
  数据库和表
  mysql层

14
 创建数据库
 增删查改

  创建数据库
  show   databases;                              对应linux的创建目录 类似ls指令
  create database  [if not exists] helloworld    上面呢      /var/lib/mysql/
  drop   database  database_name;                删除数据库  删除目录
  数据库在上层，文件系统在下层的。
  
数据库的编码
1.数据库编码集   存数据 编码集  数据库未来存储数据 utf8mb4:最多四个字节，可以表示Emoji.
2.数据校验集合   取数据 校验集  读取数据 
      进行字段比较实用的编码，本质也是一种读取数据库中数据采用的编码格式。

数据库无论对数据做任何操作，都必须保证操作和编码必须是编码一致的。

16
  show varivales like 'character_set_database';
  show variables like 'collation_database';
  show variables like 'collation_%';
  show charset;
  show collation;
  存取的规则必须一一致的。


17
  一般不会设置编码 校验规则。
create database d1;
create database d2 charset=utf8;
create database d3 character    set utf8;
create database d4 charset=utf8 collate utf8_general_ci;

default-character-set=utf8
default-collation=utf8_general_ci

18
create database tesst1 collate utf8_general_ci; 不区分大小写
create database tesst1 collate utf8_bin;        区分大小写
默认配置就行了


19
  数据库的删除 不建议直接删除数据库
  drop database if exists databae_name;
  
  查数据库
  show databases;

  使用数据库
  use database_name;

  使用数据库
  use database_name;

  查看当前在那个数据库里面
  select database();

  修改数据库的属性
  alter database database_name charset= collate=;

  show create database d1;
  上面是对数据库的增删查改

20
  数据备份和恢复，不建议直接指令复制文件夹
  mysqldump -P3306 -u root -p -B test1 > test1.sql;

  source /.. test1.sql;
  备份其实是备份的操作。
  
  备份一张表
  mysqldump -P3306 -u root -p -B test1 table_1 table_2 > test1.sql;

  show processlist;

21
  表的操作
  增删查改

22创建表
字符集
校验集
存储引擎

create table if not exists table_name(
    name1 datatype;
    name2 datatype;
    ); character set . collate , engine 


create table if not exists table_name(
    name1 datatype;
    name2 datatype;
    ); character set . collate , engine 



存储引擎文件不一样和搜索方法不一样的。
数据和索引的位置是否一样的

23数据
  use database_name;
  select database();查看在那个表里面呢
  desc table_name; 查看表数据
  show create table table_name; 当时是如何创建表的。
  show create table table_name \G; 当时是如何创建表的。
    mysql会优化您对表的操作

24
DDL,表的属性变化
  修改表
  alter table table_name
  insert into user() values();               插入数据

  alter table table_name reame to new_name;  修改表名称,重命名
  alter table table_name add                 名称 类型     新增字段
  alter table table_name modify              名称 属性     重新修改提交属性
  alter table table_name drop                名称          删除列
删除数据您的消息呢，注意备份
  alter table table_name change              属性 新属性   修改属性   
注意列名称，列的属性

rename add modify drop change

25
 删除表
 drop table table_name;

删除和修改表的结构，对程序员的伤害特别大的。注意！！！修改！！！。

//user_db：DDL学习

26
show tables;
desc table_name;
show create table table_name;
  数据类型
  1.数值类型
  2.文本，二进制类型
  3.时间日期
  4.String类型

  数值类型
  tiny   int 1字节 -127-128      0-255 半计算半规定的1
  tiny unsigned 

  small  int 2字节 -32768-32767
  medium int 3字节
         int 4字节
  big    int 8字节
  tiny   u int 1字节
  small  u int 2字节
  medium u int 3字节
         u int 4字节
  big    u int 8字节

约束
  插入数据超出范围 mysql直接拒绝 语言层面会发生截断的
  反过来，插入数据成功了一定就是合法的了。
  所以，mysql中，一把而言，数据类型本身也是一种：约束。倒逼程序员，让程序员尽肯能进行正确的
  插入 
  约束：约束使用者
  另外，你不是一个很好的使用者，mysql也能保证插入数据的合法性。
  就能够保证数据库中的数据是可预期，完整的。
约束
约束
约束
  
  mysql表建立属性列：列名称 类型在后面。
unsigned使用看场景，例如年龄。
这么多的数据类型，性能和使用之间做平衡的。

MySQL的int数据类型一共是五中

27

bit类型
bit 1---64
默认一个比特位,一个比特位只能是1,0.
最大64个比特位

select id, hex(online) from t3; 一般是不能显示出来 是ASCII存储的  注意逗号
你直接插入 a 97数据 一样的。

字符串常量必须用引号（单引号或双引号，但通常使用单引号）括起来。这里插入a必须带引号
单引号双引号都是可以的。

28
浮点类型
float
float(m,n)
  float(4,2)四位，精度是2 (-99.99, 99.99)
  mysql默认四舍五入
    在规定的精度内
  四舍五入也不能超过范围的
  float(4,2) unsigned (0, 99.99);
    减少一半的数据
默认的float数据的存储很大的

29
  decimal  规避精度损失
  decimal你存储的是什么就是什么，它也是会四舍五入的。

30
  字符串类型
  char固定长度字符串
  char(L)最大长度255；

  超过字符串长度就阻止了。
  char的字符单位 就是纯粹的字符
char纯粹的字符类型
  中国人:三个字符
  最大255个字符
  utf_8一个汉字三个字节的

  
31
  varchar(N):N代表字符串

  边长是65535字节
  字符是21845
  GBK:2字节
  UTF-8:3字节。 我们的编码是UTF-8。只能是21844
  UTF8mb4:4字节

  char    有多少用多少  类似数组
  varchar 上线不能超过  string
  
  varchar但是有1-3个字节记录数据的长度。
mysql的一行也是有限制的
所有列定义表的字节长度加起来，不能超过65535字节(64kb).

32
date      日期：年月日         生日           yyyy--mm--dd
datetime  时间：年月日time     发表评论       yyyy--mm--dd HH:ii:SS
timestamp 时间戳：timestamp    需要自动变化的 

update t11 set t1='2000-2-10';

timestamp记录任何一次操作的时间戳。
    不需要插入的
datetime时间不会变化的固定时间的
    记录你的入职事时间，基本上不能改的
    政府记录你的出生日期，基本上不会修改的
date只是用来记录日期的，记住你男朋友女朋友的生日。

结合具体的引用场景来记录时间的

33
枚举和集合
枚举
enum数据类型 只能选择一个
  只能从枚举类型里面选择一个。
  注意数字从一开始，但是也只能在枚举的范围
  可以是空的
set数据类型  多个选择多个
  可以是空的
  set('登上', '游泳', '代码', '羽毛球')；
        1       1       1       1
        计算的时候是反过来的。
  这里是位图结构的。
枚举类型是从常量0开始的。
集合多个插入时注意不要空格的，直接逗号分隔符。

NULL和空白
NULL表示没有
空白表示空串

集合是个数的比特位
可以字面值
可以比特位的计算
后面理解为为什么一开始

34
  find_in_set
枚举类型的查找
select * from t13 where gender='男';
select * from t13 where gender=1; 注意这里数字 没有引号

只有羽毛球的选手
select * from t13 where hobby='羽毛球';只有羽毛球的
这里是严格按照标准选择的

find_in_set();
find_in_set('a', 'a, b, b');
a在集合里面的
对应的元素是否在集合里面的。

select * from t13 where find_in_set('代码', hobby); 有代码就可以
select * from t13 where find_in_set( '代码', hobby) and find_in_set('足球', hobby); 有足球和代码 就可以的

select * from t13 where hobby=15;

35
表的约束：表中一定要有各种约束，通过约束，让我们未来插入数据库表中的数据是复合预期的。
约束本质是通过技术手段，插入正确的数据。
反过来，站在mysql的视角，凡是插入进来的数据，都是符合数据约束的！

约束的最终目标：保证数据的完整性和可预期性。
需要更多的约束条件，不仅仅是数据类型

36
空属性
字段的属性 null
字段的属性 not null

selectt null
selectt null+1

class_name varchar(20) not null 不能插入NULL
class_room varchar(20) not null 不能插入NULL
只要设置not null。必须存放数据的，不能是NULL。

37
default 默认值

如果设置了，用户将来插入了，有具体的数据，就插入，没有就是用默认值。
如果我们没有明确指定一列

not null default 值
只要default值 不是空的就行。
default和NOT NULL不冲突的
NOT NULL用户想插入数据是，数据合法
DEFAULT用户没有设置，直接默认值

建表的时候，MySQL优化表，默认值设置值NULL。
分清约束是NOT NULL 还是default值。
一般not null和default不放在一起

38
comment注释 列描述
约束信息，给维护数据表的人看的，软性约束。

39
zerofill约束
  约束整数
显示数据方面的约束
在显示数值时，不足指定宽度的部分用前导 0 补齐.
  创建的时候，默认给你带了。
select a, b from t17 where
  int(N) <N位给你补零，够了就不能够补充了。

默认建表的时候int(10); 是int10 数据的大小约定了能够显示多少位。

40
主键约束
主键约束：不能重复，不能为空。一张表只能有一个主键，且是整数约束，
主键默认不能够为null
凡是插入表的都是不重复的

alter table table_name drop primary key; 删除主键
alter table test_key add primary key(id);    添加主键
delete from test_key where name = "张飞2"; 删除一行的数据

一个主键可以添加给一列，或者多列。
多列就是复合主键
PRIMARY KEY (`id`,`coures`) 合起来才是主键的,必须这两个合起来是不一样的


41自增长
通常和主键搭配使用，作为逻辑主键。
auto_increment。
自增，可以自己插入增加数据，可以让它默认从零开始做增加。
AUTO_INCREMENT记录下一次应该插入多少

create table tt22( id int unsigned primary key auto_increment, name varchar(20) not null ) auto_increment=500;

一张表只能有一个自增长的。
select last_insert_id();

42
id char(20) unique comment "这是学学生的唯一键",
主键不能为空的,且是唯一的。
unique可以为空的。
唯一键，只要插入进去，就是唯一的。
主键的唯一性和其他键的唯一，也需要保证的。
主键的唯一性和其他键的唯一，也需要保证的。
43
外键约束 几张表一起约束的关系。
1.关联关系---主从
2.约束关系---该如何删除
  
1.主从的关联关系
2.产生外键约束

1.外键的作用：产生关联和增加约束

44

45
CURD操作
create retrieve update delete

create 
insert into table_name()        values(), ();
insert into table_name(feature) values(), ();

46
主键或者唯一键的冲突
insert into students values(13, 126, 'caoca', '111') on duplicate key update sn=127,name='caoca';
冲突了就更新这条数据。

47
替换指令
replace into students(sn, name, qq) values(129, '曹老板', '113455');

48
selecct * from table_name // 一般不推荐的,服务器的数据非常大的
指定列查询
select name,math,10 from exam_result; 追加一个值
select math+chinese+english from exam_result;

重命名可以带as 也可以不带as
select math+chinese+english as total from exam_result;
select name 姓名, chinese+math+english 总分 from exam_result;

select distinct math 数学 from exam_result;  去重查询

49
where 子句
null就是null
0就是0

null判断等于就是
null<=>null  专用null比较

50
练习
select * from table_name:全列的查找
select name, chinese from table_name where chinese>=80 and chinese <=90;
select name, chinese from table_name where chinese between 80 and 90
select name, chinese from table_name where chinese=58 or  chinese 59 or chinese=98 or chinese=99;
select name, chinese from table_name where chinese in(58,59,98,99);

%模糊匹配，匹配多个或者零个字符。
select name from exam_result where name  like '孙%';
select name from exam_result where name  like '孙_';

select name, chinese, math from exam_result where chinese>math;


执行顺序
select name, chinese+english+math as total from exam_result where total<200;
3                                          1                2       

select name,chinese from exam_result where chinese>80 and name not like '孙%';

select name,chinese, math, english, chinese+math+english as total from exam_result where (chinese+math+english > 200 and chinese<math and english>80) or (name like '孙_');


51
  order by;
asc
desc
select name, math from exam_result order by math desc;
select name, math from exam_result order by math asc;

52
NULL值小于任何值。

select name, math ,english, chinese from exam_result order by math desc, english desc, chinese asc;
mysql默认升序排序的

select name, chinese+math+english as total from exam_result order by order by total desc;
select name, chinese+math+english as total from exam_result order by order by total desc;
where不能使用别名，这为什么可以使用别名呢？
where不能使用别名，这为什么可以使用别名呢？
where不能使用别名，这为什么可以使用别名呢？


order by
select name, chinese+math+english as total from exam_result order by total desc;
2                                          1                3
先要有合适的数据才能排序的。

where
执行顺序
select name, chinese+english+math as total from exam_result where total<200;

select name, math from exam_result where name like '孙%' or name like '曹%' order by math desc;


分页显示数据
mysql的数据下标从零开始的
select * from exam_result limit 4;显示行数 4行

select * from exam_result limit 2, 4;从2开始，然后连续读取4行

select * from exam_result limit 5 offset 0; 0开始，然后五行。

select name, english+math+chinese as toatl from exam_result where english+math+chinese>200 order by toatl desc limit 1;

limit的本质功能只是显示，功能更加靠后的。


53
update

update exam_result set math = 80 where name='孙悟空';
小心使用的


54
delete

delete from exam_result where name='孙悟空';
delete不会将自增数据清零的

truncate不走事务的
bin log, redo log, undo log.日志信息。

55
插入查询结果

56
聚合函数
count
sum
avg
max
min
select count(*) as 总数 from exam_result;

对count的结果去重，只有一个数据，不需要去重的
select distinct count(math) from exam_result;
对math的数据去重后，然后进行数据的count
select distinct count(distinct math) from exam_result;

select sum(math) as 数学成绩总和 from exam_result;
